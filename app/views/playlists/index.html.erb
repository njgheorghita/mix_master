<h1 style="width:80%; margin-left:auto; margin-right:auto;"> the top electronic sounds of all time </h1>
<p style="width:80%; margin-left:auto; margin-right:auto;">  - according to top 10 lists aggregated from premier music blogs - </p>
<br>
<div> 
  <div class="col-md-6 dash">
    <h2> best artist </h2>
    <br>
    <% @popular_artists.each do |pa| %>
      <img src="<%= Artist.find(pa[0]).artist_image_path%>">
      <br>
      <%= link_to Artist.find(pa[0]).artist_name, artist_path(pa[0]) %>
      <br>
      <%= "with #{pa[1]} appearances across #{@playlists.count} playlists" %>
      <br>
    <% end %>
  </div>
  <div class="col-md-6 dash">
    <h2> best song </h2>
    <br>
    <% @popular_songs.each do |ps| %>
      <iframe width="560" height="315" src="<%= ps[0].youtube.gsub('watch?v=', 'embed/') %>" frameborder="0" allowfullscreen></iframe>
      <%= link_to ps[0].title, song_path(ps[0]) %>
      <br>
      <%= "appears on #{ps[1]} playlists" %>
      <br>
    <% end %>
  </div>
</div> 
<h1> playlist sources </h1>
<div>
  <% @playlists.each do |p| %>
    <div class="col-md-4">
      <%= link_to p.name, playlist_path(p) %>
      <br>
    </div>
  <% end %>
</div>
<br>
<br>
<% if @errors %>
  <%= @errors %>
<% end %>
<%= "artist = #{@artist.artist_name}" %>
<%= form_for ([@artist, @song]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.submit  %>
<% end %>